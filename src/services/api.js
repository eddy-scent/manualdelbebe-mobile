// Aquí podrás agregar las funciones de conexión al backend más adelante.
// Por ejemplo: login, registro, obtener perfil, etc.

export const api = {
  login: async (email, password) => {
    // TODO: conectar con el backend cuando esté listo
    return { success: false, message: 'Backend no configurado aún' };
  },
};
